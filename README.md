# uuid-poc
Proof of concept for resolving ID conflicts across multiple user tables from different databases by generating global unique user identifiers (UUIDs or surrogate keys) and preserving referential integrity across related tables. Simulates an ELT pipeline using Pandas, with a focus on scalable data lakehouse practices.
